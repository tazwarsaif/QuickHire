<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>View Job</title>
    <style>
      .heading {
        margin-bottom: -50px;
      }
      .smallcontainer input {
        width: 95%;
      }
      .smallcontainer textarea {
        width: 100%;
        height: 150px;
        font-size: 25px;
        font-family: sans-serif;
        padding: 5px;
      }
    </style>
    <%- include('../partials/public') %>
  </head>
  <body>
    <%- include('../partials/header') %>
    <main>
      <div class="heading">
        <h1>Edit Circular:</h1>
      </div>

      <div class="personedit">
        <form action="/recruiter/editjob?jid=<%= jid %>" method="POST">
          <div class="smallcontainer">
            <label for="title"><b>Title: </b></label>
            <input type="text" value="<%= job[0].title %>" name="title" />
          </div>
          <div class="smallcontainer">
            <label for="type"><b>Type: </b></label>
            <input type="text" value="<%= job[0].type %>" name="type" />
          </div>
          <div class="smallcontainer">
            <label for="phone"><b>Skills: </b></label>
            <input type="text" value="<%= job[0].skills %>" name="skills" />
          </div>
          <div class="smallcontainer">
            <label for="email"><b>Description: </b></label>
            <textarea name="description"><%= job[0].description %></textarea>
          </div>

          <button
            type="submit"
            class="details-btn apply"
            style="margin-top: 10px; margin-bottom: -10px; cursor: pointer"
          >
            Save Changes
          </button>
        </form>
      </div>
    </main>
    <%- include('../partials/footer') %>
  </body>
</html>
